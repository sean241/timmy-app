import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Timmy - Inscription',
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
