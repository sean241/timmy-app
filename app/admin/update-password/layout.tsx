import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Timmy - Réinitialisation mot de passe',
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
